server.error.whitelabel.enabled: false
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: ALWAYS
      
unik:
  service:
    message: Welcome to micrso-services demo..!!
